@file:Suppress("unused")
package com.hadihariri.fpkotlin.exercises.functional.optimisation

import com.hadihariri.fpkotlin.exercises.functional.basic.*

val input = listOf(2, 30, 20, 7, 3, 9, 15, 1, 4)

fun returnSumWhenOver60(input: List<Int>): Int {
    TODO("Return sum as soon as it's over 60")
    // use toSequence to convert it to a "lazy" stream
}

fun main(args: Array<String>) {

    val a = countries.flatMap { it.cities.map { it.name } }.plus(listOf("A","B"))
}